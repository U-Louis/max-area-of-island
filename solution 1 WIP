/**
 * @param {number[][]} grid
 * @return {number}
 */
var maxAreaOfIsland = function(grid) {
    
    let isles = [];
    
    //seen = 2
    
    function check(x,y){
        
        isle++;
        
        //rigth
        if(grid[x+1][y] = 1){
            check(x+1,y);
        }
        grid[x+1][y] =2;

        //left
        if(grid[x-1][y] = 1){
            check(x-1,y);
        }
        grid[x-1][y] =2;

        
        //bottom
        if(grid[x][y+1] = 1){
            check(x,y+1);
        }
        grid[x][y+1] =2;
        
        //top
        if(grid[x][y-1] = 1){
            check(x,y-1);
        }
        grid[x][y-1] =2;

    }
        
    function createIsles(x,y){
        let isle = 0;
        check(x,y);
        grid[x][y] = 2;
        isles.push(isle);
    }
    
    
    //iterate every case in the matrix
    for(i=0;i<grid.length;i++){
        for(j=0;j<grid[i].length; j++){        
            if(grid[i][j] == 1){
                createIsles(i,j);
            }   
        }
    }
    
    
    
    return Math.max(...isles);
};
